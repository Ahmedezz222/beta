<div class="page-content">
  <div class="container">
    <div class="page-header">
      <h1>{{ page.title }}</h1>
    </div>
    <div class="page-body">
      {{ page.content }}
    </div>
  </div>
</div>
