<main class="page-content" role="main">
  <div class="container">
    <header class="page-header">
      <h1>{{ page.title }}</h1>
    </header>
    <div class="page-body">
      <article class="page-content">
        {{ page.content }}
      </article>
    </div>
  </div>
</main>
